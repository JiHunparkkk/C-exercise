/*
#include<stdio.h>

extern int a, b; // 외부변수를 선언하여 컴파일러에게 다른 모듈에 외부변수가 있다는것을 알려줌

void print_value()
{
	printf("두 정수값 : %d, %d\n", a, b);
}

void exchange()
{
	int temp;

	temp = a;
	a = b;
	b = temp;
}
*/