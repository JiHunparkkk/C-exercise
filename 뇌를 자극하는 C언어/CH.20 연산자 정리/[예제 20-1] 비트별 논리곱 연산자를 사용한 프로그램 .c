/*
#include <stdio.h>

int main()
{
	char a = 'a', b = 'b'; // 문자 변수를 선언하고 초기화
	char m = (char)0xdf; // m에는 비트열 11011111이 저장된다.
	char n = 1; // n은 가장 우측의 비트만 1이다.

	printf("a&m : %c\n", a&m); // a의 비트중에 한 비트가 clear 된다.
	printf("b의 첫번째 비트 : %d\n", b&n); // b의 가장 우측비트가 0이면 결과는 0, 1이면 결과도 1이다.
	return 0;
}
*/